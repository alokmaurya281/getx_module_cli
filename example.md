# Example

Create a new module named `home`:

The command to run is `getx_module_cli home`.

The command will create the following directories and files:
- `lib/modules/home/bindings`
- `lib/modules/home/controllers`
- `lib/modules/home/views`
- `lib/modules/home/providers`


